# Copy to .env on the Pi (/opt/artyom-robo/.env) and add your key, then:
#   ssh pi systemctl restart artyom-robo
# The AI control panel appears automatically once a valid key is set.
ANTHROPIC_API_KEY=sk-ant-...
